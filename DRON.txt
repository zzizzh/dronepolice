
실시간으로 파악해야 할 것(좌표, 방향, 장애물)
안정성이 중요!


충돌 방지
{
	장애물 거리 탐색(적외선 센서 활용)
	if(일정 거리 안에 들어올 시){
		ex>제자리에 멈춰서 상승
	}	
}

돌발 상황
{
	ex> GPS 신호가 잡히지 않을 때...(근데 gps가 안잡히면 집에 어떻게 가냐)
	방향을 정확히 잡을 수 없을 때 / 목적지 방향이 계속 달라질 때(바람이 너무 심할때)

}

* 스레드에 delay, delete 함수, 반복문을 쓰면 안됨.
 스레드 객체를 delete 할 수 없는건지 다른 객체는 허용되는건지 의문.
 안된다면 명령 초기화는 어떻게 해야되지..?

드론 스레드. 

드론의 행동 제어 스레드
{
	
}
센서값 전송, 명령 수신 스레드

명령 리스트.
add, next, empty, isEmpty, getSize, getCommand

max = 3
cur = 2
